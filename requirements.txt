# Core Django + WebSocket dependencies
Django==5.0.14
asgiref==3.8.1
channels==4.0.0
channels-redis==4.2.0

# Essential AI - Groq only (lightweight)
groq

# Database & Environment
dj-database-url==3.0.1
python-dotenv==1.2.1
psycopg2-binary==2.9.9

# Web server (memory efficient)
uvicorn==0.24.0
gunicorn==21.2.0

# Django extensions
django-grappelli==4.0.1
django-allauth==65.4.1
django-crispy-forms==2.3
django-extensions==3.2.3
djangorestframework==3.15.2

# Static files & utilities
whitenoise==6.11.0
requests==2.32.3
Pillow==11.1.0
python-dateutil==2.9.0.post0


# Remove heavy ML libraries that cause memory overflow:
# - torch (2GB+) - not used in core functionality
# - transformers (500MB+) - only used in optional sentiment analysis
# - accelerate, datasets, evaluate (1GB+) - ML training libraries
# - nvidia-cuda* packages (1GB+) - CUDA support not needed for Groq API
# - bitsandbytes, sentence-transformers, seqeval - heavy ML libraries
# - scikit-learn, scipy, numpy-heavy packages - not essential for core